import { CardBorderStyleDirective } from './card-border-style.directive';

describe('CardBorderStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CardBorderStyleDirective();
    expect(directive).toBeTruthy();
  });
});
